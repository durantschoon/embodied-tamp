# Tests

Place pytest tests here as behavior is implemented. Start with meaningful checks
of task parameters and geometric outcomes, including tolerances and a known
reachable control case. Keep numerical tests independent of the Meshcat browser.

After installing `.[dev]`, run `python -m pytest` from the repository root.
This scaffold has no tests yet; pytest will report no tests collected (exit code 5).
